define("amber-domevents/Domevents-Tests", ["amber/boot", "amber_core/SUnit"], function($boot){
var $core=$boot.api,nil=$boot.nil,$recv=$boot.asReceiver,$globals=$boot.globals;
var smalltalk=$core,_st=$recv,globals=$globals;
$core.addPackage('Domevents-Tests');
$core.packages["Domevents-Tests"].transport = {"type":"amd","amdNamespace":"amber-domevents"};

$core.addClass('DomeventsTest', $globals.TestCase, [], 'Domevents-Tests');

});
